name: device_topology_view
description: A Flutter widget for visualizing device-centric network topologies.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_device_ring: ^0.2.0
  topology_view_icons: ^1.3.0
  flutter_switch_device: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
